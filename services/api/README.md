# Fastify API Services

Use this folder for standalone Fastify services when Next route handlers are not the right boundary.

Good fits:

- public API aggregation
- lead capture expansion
- partner-facing APIs
- webhook receivers
- lightweight orchestration endpoints

Avoid Express unless a future architecture decision explicitly reverses this.
