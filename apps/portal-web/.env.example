# Mapbox access token — REQUIRED.
# Mapbox is the chosen map provider for CropAutonomy. The dashboard and any other map surfaces
# will not render without this set. Get a token at https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=
