import { GridIcon } from "@gaia/ui";

import { ComingSoon } from "../_components/ComingSoon";

export default function FieldsPage() {
  return (
    <ComingSoon
      title="Fields"
      intro="Field-level history — every block, its scans, and how it's trending."
      icon={<GridIcon size={18} />}
      note="Nothing planted on this page just yet."
    />
  );
}
